# knownpacksfix
Fixes Velocity “too many known packs” disconnects during 1.21+ config phase. Client-side compatibility workaround for modded servers using NeoForge (may also work on Forge/Fabric) behind Velocity proxies.
